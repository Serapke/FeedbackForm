<h2>Login</h2>

<%= form_for(:session, url: login_path) do |f| %>
	<%= f.text_field :username, placeholder: "Username or Name" %>
	<%= f.password_field :password, placeholder: "Password" %>
	<%= f.submit "Login", class: "submit", method: "post" %>
<% end %>
